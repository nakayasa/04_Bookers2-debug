$("#comment").html("<%= escape_javascript(render partial: 'books/comment', locals: {book: @book, book_comment: @book_comment}) %>");
$("textarea").val("");

<!--render 'favorites/favorite-btn', book: @bookは、ビューで部分テンプレートを呼び出すのと同じ記述ですね。j()はescape_javascript()の省略形になります。js.erbファイルで部分テンプレートを呼び出す際に必要になります。j()でもOK-->
